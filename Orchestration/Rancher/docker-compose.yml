version: "3.8"
services:
  rancher:
    image: rancher/rancher:latest
    container_name: rancher
    restart: always
    ports:
    - "80:80/tcp"
    - "443:443/tcp"
    volumes:
    - C:\docker\volumes\Rancher\data:/var/lib/rancher
    privileged: true
